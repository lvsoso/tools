apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httpie.fullname" . }}-test-connection"
  labels:
    {{- include "httpie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httpie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
