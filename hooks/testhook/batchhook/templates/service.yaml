apiVersion: v1
kind: Service
metadata:
  name: {{ include "batchhook.fullname" . }}
  labels:
    {{- include "batchhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "batchhook.selectorLabels" . | nindent 4 }}
