apiVersion: v1
kind: Service
metadata:
  name: {{ include "demohook.fullname" . }}
  labels:
    {{- include "demohook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demohook.selectorLabels" . | nindent 4 }}
