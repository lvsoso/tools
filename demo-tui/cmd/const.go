package cmd

const (
	version = `0.0.1`
	cfgFile = `cfg.yaml`
	cfgDir  = `/.dtui/`
	cfgPath = `cfg-path`
)

// lfs cache
const (
	sourceDir   = `src-dir`
	repoRootDir = `repo-root`
	parallels   = `parallel`
)

// scan
const (
	targetDir = `target-dir`
)
